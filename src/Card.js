import React from 'react';

const Card = ({id, name, email}) => {
	return (
		<div>
			<img src='https://robohash.org/a' alt='robot'/>
			<div>
				<h2>{this.name}</h2>
				<p>{this.email}</p>
			</div>
		</div>
		);
}

export default Card;